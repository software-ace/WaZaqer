name: azqar
description: Flutter app to read Azqar.

publish_to: 'none'

version: 1.0.1+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  share: ^0.6.5+4
  shared_preferences: null
  stacked_themes: null

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
