name: wa_zaqer
description: Flutter app to read Azqar.

publish_to: 'none'

version: 1.0.1+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  share: ^2.0.1
  stacked_themes: null

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
